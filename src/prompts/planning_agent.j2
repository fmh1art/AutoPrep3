Decompose the task into operators. Each operator is executed by an autonomous coding agent with full tool access (search, read, edit, bash).

SWE cases are typically addressed by the downstream steps in general:

1. Analyze the codebase by finding and reading relevant files
2. Create a reproduction script to reproduce the issue
3. Edit the source code to resolve the issue
4. Verify your fix works by running your reproduction script again
5. Test edge cases to ensure your fix is robust

You can refer to the steps above for guidance when generating the plan of given SWE cases. But some steps can be ignored if not necessary.

Read the task instruction below carefully. Based on the specific issue described, create a plan where each operator has a issue-specific goal — NOT a generic "explore then fix" template.

**Output ONLY a JSON array:**
```json
[
  {"index": 1, "subtask": "..."},
  ...
]
```

## Task
{{ instruction }}

{% if workspace_overview %}
## Workspace
{{ workspace_overview }}
{% endif %}
