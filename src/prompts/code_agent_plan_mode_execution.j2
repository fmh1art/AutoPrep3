Read and strictly follow the implementation plan at:

  {{ plan_path }}

Execute with a phased approach consistent with the project's prompt style:

Phase 1. PLAN LOADING
  - Parse the plan and extract steps and milestones
  - Prepare required tools and environment

Phase 2. IMPLEMENTATION (Minimal Viable Changes)
  - Execute steps in order; minor adjustments are allowed but must be explained in the final summary
  - Modify only necessary files; keep changes atomic and easily reversible
  - **Explore and locate files yourself**: Do NOT wait for exact file paths from the plan. Use bash, grep, and search tools to find the relevant files and code sections.
  - **Be self-sufficient**: The plan provides high-level guidance. You are expected to explore the codebase, understand the structure, and implement changes based on your understanding.

Phase 3. VERIFICATION
  - Run the specified commands/scripts for self-check; add critical edge cases if needed
  - On failure, iterate using the rollback/retry strategy defined in the plan

Phase 4. REVIEW & SUMMARY
  - Summarize: change set, executed commands, artifacts, and verification results
  - Use the finish tool to output the final summary (include any deviations from the plan and why)

General Constraints:
  - Prefer provided tools and minimal modifications to achieve goals; avoid unnecessary dependencies
  - If you deviate from the plan, clearly record it in the final summary

## Execution Agent Responsibilities

1. **File Discovery**: The plan does NOT provide exact file paths. You MUST use explore tools (bash, grep, search_by_keyword, view_file) to locate relevant files yourself.

2. **High-Level Guidance**: The plan describes "what to do" at a conceptual level. It is YOUR job to figure out "how to do it" by exploring the codebase.

3. **Adaptive Implementation**: If the plan's approach doesn't fit the actual code structure you discover, adapt while maintaining the original intent.

Task Instruction:
{{ instruction }}

Workspace Context: {{ repo_path }}

